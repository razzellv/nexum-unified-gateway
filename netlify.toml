[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."analyze-equipment"]
  timeout = 26

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
